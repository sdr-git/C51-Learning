##模块化编程
#把动态数码管显示进行模块化，将数码管显示函数和延时函数分别模块化，建立相对应的.c.h文件，并在main.c文件中调用两个头文件
#.c文件中为相对应的函数内容，函数中所用到的变量都需要重新调用头文件，所以要把相对应的头文件加到程序的最上方
#.h文件#ifndef __DELAY_H__
#define __DELAY_H__
void Delay(unsigned int xms);
#endif
## #ifndef和#endif是成对存在的，意思是如果没有定义这个延时函数则执行这段，如果定义了将不再执行定义，这也是防止多个文件中重复定义